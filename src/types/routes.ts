// src/types/routes.ts (or wherever AppRoutes is defined)
export type AppRoutes =
  | "/"
  | "/properties"
  | "/dashboard" // Add this
  | "/dashboard/admin"
  | "/dashboard/landlord"
  | "/dashboard/tenant";
